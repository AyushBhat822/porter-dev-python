version: v1
resources:
  - name: python-sample
    source:
      name: web
    config:
      build:
        method: pack
      values:
        container:
          port: 8080
          env:
            normal:
              PORT: "8080"
